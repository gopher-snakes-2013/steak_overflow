<div id="header">
  <h1>STEAK OVERFLOW</h1>
</div>

<div id="content">
  <p>CUTS:</p>
  <ul id="topics">
    <% @topics.each do |topic| %>
      <li><%= link_to topic.title, topic_path(topic.id) %></li>
    <% end %>
  </ul>
</div>

<div id="new_cut">
  <%= link_to "Add a New Cut!", new_topic_path %>
</div>